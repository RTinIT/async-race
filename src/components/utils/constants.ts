export const SERVER_URL = 'http://127.0.0.1:3000';
export enum Path {
  garage = '/garage',
  winners = '/winners',
  page = '_page',
  limit = '_limit',
  engine = '/engine',
  started = 'started',
  stopped = 'stopped',
  drive = 'drive',
}
export const HEADERS = { 'Content-Type': 'application/json' };
